name: Build and Deploy train-ticket-reservation

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: 🔨 Build with Maven
        run: mvn clean package -DskipTests
        
      - name: 🗂 Find WAR file
        run: echo "WAR_FILE=$(ls target/*.war | head -n 1)" >> $GITHUB_ENV

      - name: ⬆ Upload WAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: war-file
          path: ${{ env.WAR_FILE }}
        
  deploy-dev:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: war-file
      
      - name: 🚀 Deploy to DEV
        env:
          URL:  ${{ secrets.TOMCAT_MANAGER_DEV }}   # e.g. http://IP:8080/manager/text
          USER: ${{ secrets.TOMCAT_USER_DEV }}
          PASS: ${{ secrets.TOMCAT_PASSWORD_DEV }}
        run: |
          WAR_FILE=$(ls *.war | head -n 1)
          echo "Deploying $WAR_FILE to DEV ($URL)"

          echo "📌 Checking Tomcat Manager access..."
          curl -v -u "$USER:$PASS" "$URL/list"

          echo "📌 Deploying WAR file..."
          curl -v -u "$USER:$PASS" --fail --show-error --upload-file "$WAR_FILE" \
            "$URL/deploy?path=/train-ticket-reservation&update=true"
            
  deploy-PRE_PROD:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: war-file
              
      - name: 🚀 Deploy to PRE_PROD
        env:
          URL:  ${{ secrets.TOMCAT_MANAGER_PRE_PROD }}   # http://IP:8080/manager/text
          USER: ${{ secrets.TOMCAT_USER_PRE_PROD }}
          PASS: ${{ secrets.TOMCAT_PASSWORD_PRE_PROD }}
        run: |
          WAR_FILE=$(ls *.war | head -n 1)
          echo "Deploying $WAR_FILE to PRE_PROD ($URL)"

          echo "📌 Checking Tomcat Manager access..."
          curl -v -u "$USER:$PASS" "$URL/list"

          echo "📌 Deploying WAR file..."
          curl -v -u "$USER:$PASS" --fail --show-error --upload-file "$WAR_FILE" \
            "$URL/deploy?path=/train-ticket-reservation&update=true"
            
  deploy-TEST:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: war-file
            
      - name: 🚀 Deploy to TEST
        env:
          URL:  ${{ secrets.TOMCAT_MANAGER_TEST }}   # http://IP:8080/manager/text
          USER: ${{ secrets.TOMCAT_USER_TEST }}
          PASS: ${{ secrets.TOMCAT_PASSWORD_TEST }}
        run: |
          WAR_FILE=$(ls *.war | head -n 1)
          echo "Deploying $WAR_FILE to TEST ($URL)"

          echo "📌 Checking Tomcat Manager access..."
          curl -v -u "$USER:$PASS" "$URL/list"

          echo "📌 Deploying WAR file..."
          curl -v -u "$USER:$PASS" --fail --show-error --upload-file "$WAR_FILE" \
            "$URL/deploy?path=/train-ticket-reservation&update=true"

  deploy-PROD:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: war-file

      - name: 🚀 Deploy to PROD
        env:
          URL:  ${{ secrets.TOMCAT_MANAGER_PROD }}   # http://IP:8080/manager/text
          USER: ${{ secrets.TOMCAT_USER_PROD }}
          PASS: ${{ secrets.TOMCAT_PASSWORD_PROD }}
        run: |
          WAR_FILE=$(ls *.war | head -n 1)
          echo "Deploying $WAR_FILE to PROD ($URL)"

          echo "📌 Checking Tomcat Manager access..."
          curl -v -u "$USER:$PASS" "$URL/list"

          echo "📌 Deploying WAR file..."
          curl -v -u "$USER:$PASS" --fail --show-error --upload-file "$WAR_FILE" \
            "$URL/deploy?path=/train-ticket-reservation&update=true"
